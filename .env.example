# GCP Configuration
GCP_PROJECT_ID=agentic-ai-mlg
GCP_REGION=us-central1

# Service Configuration
PORT=8080
ENVIRONMENT=development

# Logging
LOG_LEVEL=INFO

# Note: Secrets are managed in GCP Secret Manager, not in .env
# This file is only for non-sensitive configuration
