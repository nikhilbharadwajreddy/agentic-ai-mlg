# ========================================
# GCP PROJECT CONFIGURATION
# ========================================
# Copy this file to .env and fill in your values
# cp .env.template .env

# Project settings
PROJECT_ID="ai-agent-prod-2025"              # Must be globally unique
PROJECT_NAME="AI Agent Production"
ORGANIZATION_ID=""                           # Leave empty if no organization

# Region settings (choose one)
REGION="us-central1"                         # Iowa (recommended)
# REGION="us-east1"                          # South Carolina
# REGION="europe-west1"                      # Belgium
# REGION="asia-southeast1"                   # Singapore

# Service account names (can keep defaults)
ORCHESTRATOR_SA="orchestrator-sa"
TOOL_EXECUTOR_SA="tool-executor-sa"

# Artifact Registry
REGISTRY_NAME="ai-agent-repo"
REGISTRY_LOCATION="us-central1"

# Firestore settings
FIRESTORE_LOCATION="us-central1"
FIRESTORE_TYPE="firestore-native"

# ========================================
# OPTIONAL: EXTERNAL SERVICES
# ========================================
# These will be set in Secret Manager later

# OTP Service (e.g., Twilio)
# OTP_API_KEY="your-twilio-api-key"
# OTP_API_SECRET="your-twilio-secret"

# JWT Secret (generate with: openssl rand -base64 32)
# JWT_SECRET="your-generated-secret"

# ========================================
# NOTES
# ========================================
# - PROJECT_ID must be globally unique across all GCP
# - REGION affects latency - choose closest to your users
# - Keep this file SECRET - never commit to git
# - Add .env to .gitignore
